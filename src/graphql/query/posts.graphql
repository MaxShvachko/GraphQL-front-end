query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    data {
      id
      points
      createdAt
      text
      title
      points
      voteStatus
      textSnippet
      creator {
        id
        nick_name
      }
    }
    hasMore
  }
}
